import ProfileForm from "../component/ProfileForm";

const ModalComponent = ({ modalShow }) => {
    return (<section className={`modal__profile`}>
        < ProfileForm />
    </section>)
}

export default ModalComponent;